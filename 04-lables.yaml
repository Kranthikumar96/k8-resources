apiVersion: v1
kind: Pod
metadata:
  name: labels
  labels:
    project: roboshop
    purpose: labels-demo
    component: nginx
    tier: frontend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx