apiVersion: v1
kind: Pod
metadata:
  name: annotations
  labels:
    project: roboshop
    purpose: annotations-demo
    component: nginx
    tier: frontend
    environment: dev
  annotations:
    description: "This pod is created to demonistration the annotations in the pod"
    jenkins: "https://jenkins.com/build/job/roboshop-catalogue/3"
spec:
  containers:
  - name: nginx
    image: nginx